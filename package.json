{
  "name": "server-phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \\\\\\\"Error: no test specified\\\\\\",
    "build:ui": "rm -rf build && cd ../phonebook/ && npm run build --prod && cd .. && xcopy phonebook/build server-phonebook && cd server-phonebook",
    "deploy": "git push https://git.heroku.com/salty-citadel-27533.git",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "yusufake",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
